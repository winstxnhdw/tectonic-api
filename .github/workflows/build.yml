name: Build

on:
  pull_request:
  push:
    branches: [main]
    paths:
      - .github/workflows/build.yml
      - Dockerfile.build
      - Cargo.toml
      - src/**
      - scripts/**

permissions: {}

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7.0.1
        with:
          show-progress: false
          persist-credentials: false

      - name: Install dependenc(ies)
        run: |
          sudo apt update
          sudo apt install -y libfreetype-dev libfontconfig-dev libharfbuzz-dev

      - name: Cache Cargo
        uses: actions/cache@v6.1.0
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Lint
        run: cargo clippy

      - name: Format
        run: cargo fmt --all --check

  push:
    if: github.ref == 'refs/heads/main'
    uses: winstxnhdw/actions/.github/workflows/docker-push.yml@main
    with:
      file: Dockerfile.build
      build-args: |
        USE_FAST_CACHE=1
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
    permissions:
      packages: write

  restart:
    needs: [lint, push]
    uses: winstxnhdw/actions/.github/workflows/spaces-restart.yml@main
    secrets:
      token: ${{ secrets.HF_TOKEN }}
